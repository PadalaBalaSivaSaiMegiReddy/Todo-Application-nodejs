{
  "name": "todo-application",
  "version": "1.0.0",
  "description": "Given an `app.js` file and database file `todoApplication.db` with a table `todo`.",
  "main": "app.js",
  "author": "Megi",
  "license": "MIT",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "nodemon": "^3.0.1",
    "sqlite": "^5.1.1",
    "sqlite3": "^5.1.6"
  }
}
